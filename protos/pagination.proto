syntax = "proto3";

package unary_grpc;

option go_package="unary_grpc/pb/pagination";

message Pagination {
  uint64 total = 1;
  uint64 per_page = 2;
  uint64 current_page = 3;
  uint64 last_page = 4;
}